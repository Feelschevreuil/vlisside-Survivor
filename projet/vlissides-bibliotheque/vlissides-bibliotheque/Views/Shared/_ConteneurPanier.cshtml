@model List<TuileLivreBibliotequeVM>

@if(Model.Count!=0){
    @foreach (var card in Model)
    {
        <div class="col-4 col-md-4 col-sm-4">
            <partial name="_TuilePanierPartial" model="card"></partial>
        </div>
    }
}